FROM python:3.12-slim

WORKDIR /app

COPY docker/authz/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY services/authz/ .

EXPOSE 8083

CMD ["python", "-m", "uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8083"]
